apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: clients-api
  namespace: clients
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: clients-api
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
