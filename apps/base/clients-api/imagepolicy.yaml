apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: clients-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: clients-api
  policy:
    semver:
      range: ">=1.0.0"
