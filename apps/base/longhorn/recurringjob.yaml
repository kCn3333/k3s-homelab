apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup
  namespace: longhorn-system
spec:
  cron: "0 11 * * *"
  task: backup
  groups:
    - default
  retain: 2
  concurrency: 1
  labels:
    backup: daily
