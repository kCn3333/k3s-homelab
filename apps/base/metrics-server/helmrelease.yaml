apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: kube-system
spec:
  interval: 30m
  chart:
    spec:
      chart: metrics-server
      version: "3.12.*"
      sourceRef:
        kind: HelmRepository
        name: metrics-server
        namespace: flux-system
      interval: 12h
  values:
    args:
      - --cert-dir=/tmp
      - --secure-port=4443
      - --kubelet-preferred-address-types=InternalIP
      - --kubelet-insecure-tls
      - --metric-resolution=25s
    hostNetwork:
      enabled: false
    containerPort: 4443
    livenessProbe:
      httpGet:
        port: 4443
        path: /livez
        scheme: HTTPS
    readinessProbe:
      enabled: true
      httpGet:
        path: /readyz
        port: 4443
        scheme: HTTPS
      initialDelaySeconds: 20
